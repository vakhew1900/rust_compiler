fn main() {
    let mut number = 10;

    loop {

        if number <= 3 {
            break;
        }

        number = number * (number + 12) / 33 % 3 - 2 - 3 + (0 - 3);
    }
    13 * 14 - 10 > 12 + 10 / 3;
}
